# Games Platform

This document is historical context only.

The former games surface is not part of the current reduced Lemon platform.
`lemon_games`, the related control-plane REST API, and the spectator web UI were
removed during the nano-platform consolidation.

If game support returns in the future, it should be reintroduced as an explicit
side product rather than as part of the canonical headless runtime.
